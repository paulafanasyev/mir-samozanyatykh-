import { API_BASE_URL } from '../api/client'
type Props={size?:'sm'|'md'|'lg';interactive?:boolean;className?:string}
export default function SvetlanaAvatar({size='md',className=''}:Props){const dims=size==='lg'?'h-[min(72vh,720px)] min-h-[420px] w-full':size==='sm'?'h-12 w-12':'h-24 w-24';const portraitUrl=`${API_BASE_URL}/static/svetlana/face.png`;return <div className={`${dims} ${className} relative overflow-hidden rounded-2xl border border-slate-200 bg-slate-950 shadow-lg`} aria-label="Светлана"><img src={portraitUrl} alt="Светлана — ИИ-помощник" className="h-full w-full object-cover object-top" loading={size==='lg'?'eager':'lazy'}/><div className="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/20 via-transparent to-white/5"/><span className="absolute bottom-2 right-2 h-3 w-3 rounded-full border-2 border-white bg-emerald-500 shadow" title="Светлана доступна"/></div>}
export function commandSvetlana(_iframe:HTMLIFrameElement|null,_payload:Record<string,unknown>){/* 3D runtime is optional; voice/chat do not depend on it. */}
