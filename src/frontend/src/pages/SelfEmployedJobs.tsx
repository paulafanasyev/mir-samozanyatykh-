import { useEffect, useState } from 'react'
import { ExternalLink, Search, BriefcaseBusiness, RefreshCw } from 'lucide-react'
import apiClient from '../api/client'
interface Job { id:string; title:string; company:string; region:string; salary:string; employment:string; schedule:string; duty:string; url:string; created_at:string }
export default function SelfEmployedJobs(){const[q,setQ]=useState('самозанятый');const[jobs,setJobs]=useState<Job[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const load=async()=>{setLoading(true);setError('');try{const r=await apiClient.get('/api/jobs/self-employed',{params:{q,limit:30}});setJobs(r.data.items||[])}catch{setError('Не удалось получить открытые данные «Работы России». Попробуйте ещё раз.')}finally{setLoading(false)}};useEffect(()=>{void load()},[]);return <div className="mx-auto max-w-6xl space-y-6"><div className="card"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><div className="eyebrow mb-2">Работа России</div><h1 className="text-3xl font-black tracking-tight text-slate-950">Предложения для самозанятых</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Показываем только записи из открытого API, где в опубликованных данных явно встречается самозанятость, НПД или ГПХ. Источник — trudvsem.ru.</p></div><a href="https://trudvsem.ru/" target="_blank" rel="noopener noreferrer" className="btn-secondary shrink-0"><ExternalLink className="h-4 w-4"/>Работа России</a></div><div className="mt-6 flex gap-2"><div className="flex flex-1 items-center rounded-xl border border-slate-200 bg-white px-3"><Search className="h-4 w-4 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')void load()}} className="input border-0 shadow-none" placeholder="Например: дизайнер, программист, фотограф"/></div><button onClick={()=>void load()} className="btn-brand"><Search className="h-4 w-4"/>Найти</button></div></div>{error&&<div className="rounded-2xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}{loading?<div className="card flex items-center justify-center py-16 text-slate-400"><RefreshCw className="mr-2 h-5 w-5 animate-spin"/>Обновляем предложения…</div>:jobs.length===0?<div className="card py-16 text-center"><BriefcaseBusiness className="mx-auto h-10 w-10 text-slate-300"/><h2 className="mt-4 font-black text-slate-800">Явных предложений пока не найдено</h2><p className="mt-2 text-sm text-slate-500">Попробуйте другой запрос. Мы не подменяем обычные трудовые вакансии предложениями для самозанятых.</p></div>:<div className="grid gap-4">{jobs.map(job=><article key={job.id} className="card transition hover:-translate-y-0.5 hover:shadow-lg"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><h2 className="text-lg font-black text-slate-950">{job.title}</h2><div className="mt-1 text-sm text-slate-500">{job.company||'Работодатель не указан'} · {job.region||'Регион не указан'}</div></div><a href={job.url} target="_blank" rel="noopener noreferrer" className="btn-secondary shrink-0">Открыть <ExternalLink className="h-4 w-4"/></a></div><div className="mt-4 grid gap-3 text-sm sm:grid-cols-3"><div><span className="text-slate-400">Оплата</span><div className="font-semibold text-slate-700">{job.salary||'не указана'}</div></div><div><span className="text-slate-400">Формат</span><div className="font-semibold text-slate-700">{job.employment||job.schedule||'не указан'}</div></div><div><span className="text-slate-400">Источник</span><div className="font-semibold text-slate-700">trudvsem.ru</div></div></div>{job.duty&&<p className="mt-4 text-sm leading-6 text-slate-500">{job.duty.slice(0,700)}</p>}</article>)}</div>}</div>}
