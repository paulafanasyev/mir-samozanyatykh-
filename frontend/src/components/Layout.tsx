import { Outlet, Link, useLocation, useNavigate } from 'react-router-dom'  import { useAuthStore } from '../stores/authStore'  import { useState, useEffect } from 'react'  import {    LayoutDashboard, FileText, Users, Handshake, Receipt,    Settings, LogOut, Menu, X, Bell, Calculator,    Calendar, BookOpen, Sparkles, UserCircle, Shield,    Mail, BarChart3, CreditCard  } from 'lucide-react'    const navItems = [    { to: '/dashboard', label: 'Панель', icon: LayoutDashboard },    { to: '/invoices', label: 'Счета', icon: FileText },    { to: '/clients', label: 'Клиенты', icon: Users },    { to: '/deals', label: 'Сделки', icon: Handshake },    { to: '/tasks', label: 'Задачи', icon: Sparkles },    { to: '/calendar', label: 'Календарь', icon: Calendar },    { to: '/accounting', label: 'Бухгалтерия', icon: Calculator },    { to: '/receipt-check', label: 'Чеки ФНС', icon: Receipt },    { to: '/email-campaigns', label: 'Рассылки', icon: Mail },    { to: '/integrations', label: 'Интеграции', icon: BarChart3 },    { to: '/docs', label: 'Документация', icon: BookOpen },  ]    export default function Layout() {    const [sidebarOpen, setSidebarOpen] = useState(false)    const [unreadCount, setUnreadCount] = useState(0)    const { user, logout } = useAuthStore()    const location = useLocation()    const navigate = useNavigate()      useEffect(() => {      const fetchUnread = async () => {        try {          const { default: apiClient } = await import('../api/client')          const res = await apiClient.get('/api/notifications/unread-count')          setUnreadCount(res.data.unread_count || 0)        } catch { /* ignore */ }      }      fetchUnread()      const interval = setInterval(fetchUnread, 30000)      return () => clearInterval(interval)    }, [])      const handleLogout = () => {      logout()      navigate('/login')    }      const isAdmin = user?.is_admin || user?.is_moderator      return (      <div className='min-h-screen bg-slate-50 flex'>        {/* Mobile overlay */}        {sidebarOpen && (          <div className='fixed inset-0 bg-black/50 z-40 lg:hidden' onClick={() => setSidebarOpen(false)} />        )}          {/* Sidebar */}        <aside className={`fixed lg:static inset-y-0 left-0 z-50 w-64 bg-slate-900 text-white transform transition-transform lg:transform-none ${sidebarOpen ? 'translate-x-0' : '-translate-x-full'} flex flex-col`}>          <div className='p-4 flex items-center justify-between border-b border-slate-800'>            <Link to='/' className='text-lg font-bold flex items-center gap-2'>              <span className='w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center text-sm'>МС</span>              <span className='hidden sm:inline'>Мир Самозанятых</span>            </Link>            <button onClick={() => setSidebarOpen(false)} className='lg:hidden p-1 hover:bg-slate-800 rounded'>              <X className='w-5 h-5' />            </button>          </div>            <nav className='px-2 py-3 space-y-0.5 flex-1 overflow-y-auto'>            {navItems.map((item) => {              const Icon = item.icon              const isActive = location.pathname === item.to || location.pathname.startsWith(`${item.to}/`)              return (                <Link                  key={item.to}                  to={item.to}                  onClick={() => setSidebarOpen(false)}                  className={`flex items-center gap-3 px-3 py-2 rounded-lg transition-colors text-sm ${isActive ? 'bg-blue-600 text-white' : 'text-slate-300 hover:bg-slate-800'}`}                >                  <Icon className='w-4 h-4 flex-shrink-0' />                  <span>{item.label}</span>                </Link>              )            })}              {isAdmin && (              <>                <div className='pt-4 pb-1 px-3 text-xs text-slate-500 uppercase tracking-wider'>Админ</div>                <Link to='/admin' onClick={() => setSidebarOpen(false)} className={`flex items-center gap-3 px-3 py-2 rounded-lg transition-colors text-sm ${location.pathname === '/admin' ? 'bg-blue-600 text-white' : 'text-slate-300 hover:bg-slate-800'}`}>                  <Shield className='w-4 h-4' />                  <span>Панель админа</span>                </Link>                <Link to='/admin/audit-logs' onClick={() => setSidebarOpen(false)} className={`flex items-center gap-3 px-3 py-2 rounded-lg transition-colors text-sm ${location.pathname === '/admin/audit-logs' ? 'bg-blue-600 text-white' : 'text-slate-300 hover:bg-slate-800'}`}>                  <BarChart3 className='w-4 h-4' />                  <span>Audit логи</span>                </Link>              </>            )}          </nav>            <div className='p-3 border-t border-slate-800'>            <Link to='/profile' className='flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-slate-800 transition-colors mb-2'>              <div className='w-8 h-8 rounded-full bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center text-sm font-bold flex-shrink-0'>                {user?.full_name?.[0] || user?.email?.[0] || 'U'}              </div>              <div className='flex-1 min-w-0'>                <p className='text-sm font-medium truncate'>{user?.full_name || user?.email}</p>                <p className='text-xs text-slate-400 capitalize'>{user?.tier || 'free'}</p>              </div>            </Link>            <button onClick={handleLogout} className='flex items-center gap-2 text-sm text-slate-400 hover:text-white transition-colors w-full px-3 py-2 rounded-lg hover:bg-slate-800'>              <LogOut className='w-4 h-4' />              Выйти            </button>          </div>        </aside>          {/* Main content */}        <div className='flex-1 flex flex-col min-w-0 min-h-screen'>          {/* Header */}          <header className='bg-white border-b border-slate-200 px-4 py-3 flex items-center justify-between sticky top-0 z-30'>            <button onClick={() => setSidebarOpen(true)} className='lg:hidden p-2 hover:bg-slate-100 rounded-lg'>              <Menu className='w-5 h-5' />            </button>            <div className='flex items-center gap-2'>              <Link to='/notifications' className='relative p-2 text-slate-600 hover:text-slate-900 hover:bg-slate-100 rounded-lg transition-colors'>                <Bell className='w-5 h-5' />                {unreadCount > 0 && (                  <span className='absolute top-1 right-1 w-4 h-4 bg-red-500 text-white text-[10px] font-bold rounded-full flex items-center justify-center'>                    {unreadCount > 9 ? '9+' : unreadCount}                  </span>                )}              </Link>              <Link to='/profile' className='p-2 text-slate-600 hover:text-slate-900 hover:bg-slate-100 rounded-lg transition-colors lg:hidden'>                <UserCircle className='w-5 h-5' />              </Link>            </div>          </header>            {/* Page content */}          <main className='flex-1 p-4 lg:p-6 overflow-auto'>            <Outlet />          </main>            {/* Footer */}          <footer className='bg-white border-t border-slate-200 px-4 py-3 text-center text-xs text-slate-400'>            Мир Самозанятых v8.5.0 · АНО ЦПС ИНН 9724016805          </footer>        </div>      </div>    )  }